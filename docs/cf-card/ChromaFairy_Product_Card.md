# Chroma Fairy — product card (for avintph.com red slot)

*Ready to slot into the empty Products card once Chroma Fairy is live. Match the existing card styling + the animated-icon treatment.*

**Icon:** `chroma-fairy-icon.svg` (this folder) — simple red line fairy in the set's language. Uses `currentColor` so it themes with the red; give it the same animation as the other card icons.

**Title:** Chroma Fairy

**Badge:** `Client · Live` (green) once live — flags it as our first shipped *client* project (vs. our own products). While waiting on Sam: `Launching` (amber).

**Tag:** `WEB`

**Description (card):**
> A catalogue and studio for fluid-abstract artist Samantha Ty — animated shop, sales and customer CRM, and a living-painting background built from her own works.

**Link:** `Learn more ↗` → chromafairy.com (or the vercel URL until the domain is live).

**Longer "what we built" blurb (hover / detail / case study):**
> Chroma Fairy — Built for a rising Filipino fluid-abstract artist: a public shop with SEO and commission flows, plus a role-gated studio for catalogue management, atomic sales/orders, a customer CRM, scheduling, and insights. Signature touch: a custom WebGL background that morphs through her real paintings as you scroll. Next.js · Supabase · Vercel.

*(For public/blog use, keep the artist's name off — "a rising Filipino fluid-abstract artist" — and let people connect the dots.)*
